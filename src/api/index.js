import media from './media';

/* If the app was to be extended with another feature and new API calls became
necessary, it'll be easy to add them and expand the app later on.
For example, a new API module called genre with its own methods could be added
and exported through here. */
export default {
  media
};
